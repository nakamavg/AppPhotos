<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    
    <!-- Configuración específica para MIUI -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" 
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:icon="@drawable/ic_day"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_day"
        android:allowBackup="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nemergentprueba"
        tools:replace="android:icon, android:roundIcon">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- Categoría HOME para aparecer como posible lanzador predeterminado -->
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        
        <!-- Para asegurar visibilidad en MIUI -->
        <meta-data
            android:name="android.max_aspect"
            android:value="2.4" />
        <meta-data 
            android:name="android.notch_support" 
            android:value="true"/>
    </application>
</manifest>